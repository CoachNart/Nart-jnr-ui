"use client";

import Link from "next/link";
import { useCallback, useEffect, useState } from "react";
import { ArrowLeft, RefreshCw, ShieldAlert } from "lucide-react";
import StandaloneShell from "./standalone-shell";
import { auth } from "../lib/firebase";
import { kitsetupsAuthFetch } from "../lib/api";

type AnyData = Record<string, any>;
const fmt=(v:any,d=2)=>{const n=Number(v);return Number.isFinite(n)?n.toLocaleString(undefined,{maximumFractionDigits:d}):"—"};
const text=(v:any)=>String(v??"—").replaceAll("_"," ").toUpperCase();
const value=(...v:any[])=>v.find(x=>x!==undefined&&x!==null&&x!=="")??"—";

function Box({title,children,className=""}:{title:string;children:React.ReactNode;className?:string}){return <section className={`rounded-3xl border border-zinc-900 bg-[#080a0c] p-5 sm:p-6 ${className}`}><h2 className="mb-4 text-sm font-semibold text-zinc-100">{title}</h2>{children}</section>}
function Metric({label,value:val,tone="neutral"}:{label:string;value:any;tone?:string}){const c=tone==="green"?"text-emerald-300":tone==="red"?"text-red-300":"text-zinc-100";return <div className="rounded-2xl border border-zinc-900 bg-black/20 p-4"><p className="font-mono text-[7px] tracking-[.16em] text-zinc-600">{label}</p><p className={`mt-2 text-sm font-semibold ${c}`}>{val}</p></div>}
function List({items}:{items:any}){const arr=Array.isArray(items)?items:items?[items]:[];return arr.length?<div className="space-y-2">{arr.slice(0,12).map((x:any,i)=><div key={i} className="rounded-xl border border-zinc-900 bg-black/10 px-3 py-3 text-xs text-zinc-400">{typeof x==="string"?x:<><span className="font-medium text-zinc-200">{fmt(x?.price??x?.level??x?.value)}</span>{x?.timeframe?<span className="ml-2 font-mono text-[8px] text-zinc-600">{text(x.timeframe)}</span>:null}{x?.type?<span className="ml-2 font-mono text-[8px] text-zinc-600">{text(x.type)}</span>:null}{x?.reason?<p className="mt-1.5 leading-5 text-zinc-500">{x.reason}</p>:null}</>}</div>)}</div>:<p className="text-xs text-zinc-700">No engine data supplied.</p>}
function Lifecycle({setup,analysis}:{setup:AnyData;analysis:AnyData}){const lc=setup?.lifecycle||analysis?.lifecycle||{};const status=text(lc?.status||setup?.status||analysis?.decision?.status||"WAIT");const history=Array.isArray(lc?.history)?lc.history:Array.isArray(lc?.events)?lc.events:[];return <Box title="Lifecycle"><div className="flex flex-wrap items-center gap-2"><span className="rounded-full border border-cyan-400/15 bg-cyan-400/[.04] px-3 py-2 font-mono text-[8px] tracking-[.1em] text-cyan-200">{status}</span>{lc?.outcome?<span className="font-mono text-[8px] tracking-[.1em] text-zinc-600">OUTCOME · {text(lc.outcome)}</span>:null}</div>{history.length?<div className="mt-5 flex items-center gap-1 overflow-x-auto pb-1">{history.map((event:any,i:number)=><div key={i} className="flex shrink-0 items-center gap-1"><span className="rounded-full border border-emerald-400/20 bg-emerald-400/[.04] px-2.5 py-1.5 font-mono text-[7px] tracking-[.08em] text-emerald-300">{text(event?.status||event?.state||event)}</span>{i<history.length-1?<span className="text-zinc-800">→</span>:null}</div>)}</div>:<p className="mt-4 text-xs text-zinc-600">The engine has not supplied a lifecycle event history; the current canonical state is shown above.</p>}</Box>}
function Context({analysis}:{analysis:AnyData}){const c=analysis?.evidence?.context||analysis?.technical?.context||analysis?.context||{};const s=analysis?.evidence?.structures||analysis?.technical?.structure||analysis?.structures||{};const m=analysis?.evidence?.momentum||analysis?.technical?.momentum||analysis?.momentum||{};return <Box title="Market Context"><div className="space-y-2">{["1w","1d","4h","1h","30m"].map(tf=>{const a=c?.timeframes?.[tf]||c?.[tf]||{};const st=s?.[tf]||{};const mo=m?.[tf]||m?.timeframes?.[tf]||{};const role=tf==="1w"?"MACRO":tf==="1d"?"PRIMARY":tf==="4h"?"INTERMEDIATE":tf==="1h"?"TRADE":"EXECUTION";return <div key={tf} className="rounded-2xl border border-zinc-900 bg-black/10 p-4"><div className="flex justify-between"><span className="font-mono text-[9px] text-zinc-200">{tf.toUpperCase()} <span className="ml-2 text-[7px] text-zinc-600">{role}</span></span><span className="font-mono text-[7px] text-zinc-600">{text(value(a?.regime,a?.marketRegime))}</span></div><div className="mt-3 grid grid-cols-3 gap-2"><Metric label="TREND" value={text(value(a?.trend,st?.direction))}/><Metric label="STRUCTURE" value={text(value(st?.structure,st?.breaks?.latest?.kind))}/><Metric label="MOMENTUM" value={text(value(mo?.direction,mo?.bias))}/></div><p className="mt-3 text-xs leading-5 text-zinc-500">{value(a?.context,a?.summary,a?.reason,st?.summary,mo?.summary)}</p></div>})}</div></Box>}
function Structure({analysis}:{analysis:AnyData}){const s=analysis?.evidence?.structures||analysis?.technical?.structure||analysis?.structures||{};return <Box title="Structure Analysis"><div className="space-y-3">{["1w","1d","4h","1h","30m"].map(tf=>{const a=s?.[tf]||{};const b=a?.breaks?.latest||{};return <div key={tf} className="rounded-2xl border border-zinc-900 bg-black/10 p-4"><div className="flex items-center justify-between"><span className="font-mono text-[9px] text-zinc-200">{tf.toUpperCase()}</span><span className="font-mono text-[8px] text-cyan-300">{text(value(a?.structure,a?.direction,a?.trend))}</span></div><div className="mt-3 grid grid-cols-2 gap-2 sm:grid-cols-4"><Metric label="BOS" value={text(value(a?.bos,b?.kind==="BOS"?b.kind))}/><Metric label="CHoCH" value={text(value(a?.choch,b?.kind==="CHoCH"?b.kind))}/><Metric label="PROTECTED HIGH" value={fmt(value(a?.protectedHigh?.price,a?.protectedHigh?.level))}/><Metric label="PROTECTED LOW" value={fmt(value(a?.protectedLow?.price,a?.protectedLow?.level))}/></div><p className="mt-3 text-xs text-zinc-500">Structural break: {fmt(value(b?.level,b?.price))} · {text(value(b?.kind,b?.type))}</p></div>})}</div></Box>}
function Liquidity({analysis}:{analysis:AnyData}){const l=analysis?.evidence?.liquidity||analysis?.technical?.liquidity||analysis?.liquidity||{};return <Box title="Liquidity Analysis"><div className="grid gap-3 sm:grid-cols-2">{[["BUY-SIDE",value(l?.buySide,l?.buySideLiquidity)],["SELL-SIDE",value(l?.sellSide,l?.sellSideLiquidity)],["EQH",value(l?.eqh,l?.equalHighs)],["EQL",value(l?.eql,l?.equalLows)],["SWEPT",value(l?.swept,l?.sweptLiquidity)],["UNSWEPT",value(l?.unswept,l?.unsweptLiquidity)]].map(([k,v])=><div key={String(k)} className="rounded-2xl border border-zinc-900 bg-black/10 p-4"><p className="font-mono text-[7px] tracking-[.16em] text-zinc-600">{k}</p><div className="mt-3"><List items={v}/></div></div>)}</div><div className="mt-3 grid gap-2 sm:grid-cols-3"><Metric label="NEAREST" value={fmt(value(l?.nearestMeaningful?.price,l?.nearest?.price,l?.nearestMeaningful))}/><Metric label="NEXT" value={fmt(value(l?.nextMeaningful?.price,l?.next?.price,l?.nextMeaningful))}/><Metric label="EXTERNAL" value={fmt(value(l?.external?.price,l?.externalLiquidity?.price,l?.external))}/></div><p className="mt-4 text-xs leading-5 text-zinc-500">{value(l?.summary,l?.reason,analysis?.reasons?.join?.(" "))}</p></Box>}
function EntryStopTargets({analysis,setup}:{analysis:AnyData;setup:AnyData}){const e=analysis?.evidence?.entry||analysis?.levels||{};const st=analysis?.evidence?.stop||{};const targets=Array.isArray(setup?.targets)?setup.targets:Array.isArray(analysis?.levels?.targets)?analysis.levels.targets:Array.isArray(analysis?.evidence?.targets)?analysis.evidence.targets:[];return <div className="grid gap-4 lg:grid-cols-2"><Box title="Entry Analysis"><div className="grid grid-cols-2 gap-2"><Metric label="MODEL" value={text(value(e?.model,setup?.setupType))}/><Metric label="TIMEFRAME" value={text(value(e?.timeframe,setup?.timeframe,analysis?.timeframe))}/><Metric label="ENTRY" value={`$${fmt(value(e?.price,setup?.entry,analysis?.levels?.entry))}`} tone="green"/><Metric label="BREAK LEVEL" value={`$${fmt(value(e?.breakLevel,e?.level))}`}/></div><p className="mt-3 rounded-2xl border border-zinc-900 bg-black/10 p-4 text-xs leading-5 text-zinc-500">{value(e?.confirmation,e?.reason,setup?.thesis?.entry)}</p></Box><Box title="Stop Analysis"><div className="grid grid-cols-2 gap-2"><Metric label="STOP" value={`$${fmt(value(st?.stop,setup?.stop,analysis?.levels?.stop))}`} tone="red"/><Metric label="STRUCTURAL LEVEL" value={`$${fmt(value(st?.protectedLevel,st?.level))}`}/><Metric label="DISTANCE" value={setup?.risk!=null?`${fmt(setup.risk)} pts`:"—"}/><Metric label="INVALIDATION" value={text(value(st?.type,st?.reason,setup?.thesis?.invalidation))}/></div><p className="mt-3 rounded-2xl border border-red-400/10 bg-red-400/[.025] p-4 text-xs leading-5 text-zinc-500">{value(st?.reason,setup?.thesis?.invalidation,analysis?.risk?.warnings?.[0])}</p></Box><Box title="Target Analysis" className="lg:col-span-2"><div className="grid gap-3 lg:grid-cols-3">{targets.slice(0,3).map((t:any,i:number)=><div key={i} className="rounded-2xl border border-zinc-900 bg-black/10 p-4"><div className="flex justify-between"><span className="font-mono text-[8px] text-zinc-500">TP{i+1}</span><span className="font-mono text-[8px] text-emerald-300">{t?.riskReward!=null?`${fmt(t.riskReward)} R`:"—"}</span></div><p className="mt-3 text-xl font-semibold text-emerald-300">${fmt(t?.price)}</p><p className="mt-3 text-xs text-zinc-500">Liquidity: {text(value(t?.liquidityClass,t?.liquidityType,t?.source))}</p><p className="mt-1 text-xs text-zinc-500">Timeframe: {text(t?.timeframe)}</p><p className="mt-2 text-xs leading-5 text-zinc-600">{value(t?.reason,t?.explanation,t?.description)}</p></div>)}{!targets.length?<p className="text-xs text-zinc-700">No published targets.</p>:null}</div></Box></div>}
function Quality({analysis,setup}:{analysis:AnyData;setup:AnyData}){const q=setup?.quality||analysis?.setup||{};const c=q?.components||{};return <Box title="Quality Breakdown"><div className="grid gap-2 sm:grid-cols-2 lg:grid-cols-4">{Object.entries(c).map(([k,v]:any)=><div key={k} className="rounded-2xl border border-zinc-900 bg-black/10 p-4"><div className="flex justify-between"><span className="font-mono text-[7px] text-zinc-600">{text(k)}</span><span className="text-sm font-semibold text-zinc-200">{typeof v==="object"?fmt(v?.score??v?.value):fmt(v)}</span></div><p className="mt-2 text-[10px] leading-4 text-zinc-600">{typeof v==="object"?value(v?.reason,v?.explanation):"Engine quality component."}</p></div>)}</div><div className="mt-3 grid grid-cols-3 gap-2"><Metric label="SCORE" value={`${fmt(q?.score??analysis?.setup?.score,0)} / 100`}/><Metric label="GRADE" value={q?.grade||analysis?.setup?.grade||"—"}/><Metric label="CONFIDENCE" value={text(q?.confidence||analysis?.setup?.confidence)}/></div></Box>}

export default function SetupAnalysisPage({setupId}:{setupId:string}){
  const[setup,setSetup]=useState<AnyData|null>(null),[analysis,setAnalysis]=useState<AnyData|null>(null),[loading,setLoading]=useState(true),[error,setError]=useState("");
  const load=useCallback(async()=>{const u=auth.currentUser;if(!u){setError("Sign in to view setup analysis.");setLoading(false);return}setLoading(true);setError("");try{const token=await u.getIdToken();const r=await kitsetupsAuthFetch(`/api/analysis/setups/${encodeURIComponent(setupId)}`,token);const body=await r.json().catch(()=>({}));if(!r.ok||!body?.ok)throw new Error(body?.error||"Unable to load setup analysis");setSetup(body.data?.setup||null);setAnalysis(body.data?.analysis||null)}catch(e){setError(e instanceof Error?e.message:"Unable to load setup analysis.")}finally{setLoading(false)}},[setupId]);
  useEffect(()=>{void auth.authStateReady().then(load)},[load]);
  if(loading)return <StandaloneShell title="SETUP ANALYSIS"><div className="h-[70vh] animate-pulse rounded-3xl border border-zinc-900 bg-zinc-950/70"/></StandaloneShell>;
  if(error||!setup||!analysis)return <StandaloneShell title="SETUP ANALYSIS"><div className="rounded-3xl border border-zinc-900 bg-zinc-950/70 p-8 text-center"><p className="text-sm text-zinc-300">{error||"Setup analysis is unavailable."}</p><Link href="/setups" className="mt-5 inline-flex items-center gap-2 rounded-xl border border-zinc-800 px-4 py-2 text-xs text-zinc-300"><ArrowLeft className="h-3.5 w-3.5"/>Back to setups</Link></div></StandaloneShell>;
  const targets=Array.isArray(setup.targets)?setup.targets.slice(0,3):[];const direction=text(setup.direction);const status=text(setup?.lifecycle?.status||analysis?.lifecycle?.status||setup.status);return <StandaloneShell title="SETUP ANALYSIS"><div className="mb-6 flex items-center justify-between"><Link href="/setups" className="inline-flex items-center gap-2 text-xs text-zinc-500 hover:text-zinc-200"><ArrowLeft className="h-3.5 w-3.5"/>All setups</Link><button onClick={()=>void load()} className="flex h-9 w-9 items-center justify-center rounded-xl border border-zinc-800 text-zinc-500 hover:border-cyan-400/20 hover:text-cyan-300"><RefreshCw className="h-3.5 w-3.5"/></button></div><header className="rounded-3xl border border-zinc-800/90 bg-[#07090b] p-5 sm:p-7"><div className="flex flex-col gap-4 lg:flex-row lg:items-end lg:justify-between"><div><div className="flex flex-wrap items-center gap-3"><h1 className="text-3xl font-semibold tracking-[-.05em] text-zinc-100">{setup.symbol}</h1><span className={`text-base font-semibold ${direction==="LONG"?"text-emerald-300":direction==="SHORT"?"text-red-300":"text-zinc-500"}`}>{direction}</span><span className="rounded-full border border-zinc-800 px-2.5 py-1 font-mono text-[7px] tracking-[.13em] text-zinc-400">{text(setup.setupType)}</span></div><p className="mt-2 text-xs text-zinc-600">Complete engine intelligence for this setup.</p></div><div className="flex flex-wrap gap-2"><span className="rounded-full border border-cyan-400/15 bg-cyan-400/[.04] px-3 py-2 font-mono text-[8px] text-cyan-200">{text(setup?.quality?.confidence||analysis?.setup?.confidence)} CONFIDENCE</span><span className="rounded-full border border-zinc-800 px-3 py-2 font-mono text-[8px] text-zinc-300">GRADE {setup?.quality?.grade||analysis?.setup?.grade||"—"}</span><span className="rounded-full border border-zinc-800 px-3 py-2 font-mono text-[8px] text-zinc-300">{status}</span></div></div><div className="mt-6 grid grid-cols-2 gap-2 sm:grid-cols-3 lg:grid-cols-6"><Metric label="MARKET PRICE" value={`$${fmt(setup.price||analysis?.market?.price)}`}/><Metric label="ENTRY" value={`$${fmt(setup.entry||analysis?.levels?.entry)}`} tone="green"/><Metric label="STOP" value={`$${fmt(setup.stop||analysis?.levels?.stop)}`} tone="red"/><Metric label="TP1" value={`$${fmt(targets[0]?.price)}`} tone="green"/><Metric label="TP2" value={`$${fmt(targets[1]?.price)}`} tone="green"/><Metric label="TP3" value={`$${fmt(targets[2]?.price)}`} tone="green"/></div></header><div className="mt-4 space-y-4"><Lifecycle setup={setup} analysis={analysis}/><Context analysis={analysis}/><Structure analysis={analysis}/><Liquidity analysis={analysis}/><EntryStopTargets analysis={analysis} setup={setup}/><Quality analysis={analysis} setup={setup}/><Box title="Setup Thesis"><div className="rounded-2xl border border-cyan-300/10 bg-cyan-300/[.025] p-5 text-sm leading-7 text-zinc-300">{value(setup?.thesis?.summary,setup?.thesis?.structural,setup?.thesis?.liquidity,setup?.thesis?.entry,analysis?.reasons?.join?.(" "))}</div></Box><Box title="Invalidation & Risks"><div className="flex gap-3 rounded-2xl border border-red-400/10 bg-red-400/[.025] p-5"><ShieldAlert className="h-4 w-4 shrink-0 text-red-300"/><div><p className="text-sm font-medium text-zinc-200">What invalidates this setup?</p><p className="mt-2 text-xs leading-5 text-zinc-500">{value(setup?.thesis?.invalidation,analysis?.risk?.warnings?.join?.(" "),analysis?.reasons?.find?.((x:any)=>/invalid|conflict|risk/i.test(String(x))))}</p></div></div></Box></div></StandaloneShell>;
}
